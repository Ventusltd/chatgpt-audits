# 04 — Seams and copied-Claude-study cross-check

> **REVIEW STATUS: UNREVIEWED**  
> Classification: `observed`  
> This is quarantined study output. It is not installed, trusted or published.

The copied study is treated as a hypothesis corpus, not as current truth. Every row below was tested against exact commits captured at run start.

| Claim | Result | Statement | Note |
|---|---|---|---|
| `CROSSCHECK-001` | `observed` | PipelineNews still emits the pre-move root GridAtlas release route. | The copied study's producer-side string is present at the pinned commit. |
| `CROSSCHECK-002` | `observed` | GridAtlas declares `/gridatlas/atlas/` as its stable live route. | Current selected pointer/composition files contain the stable route. |
| `CROSSCHECK-003` | `observed` | The `202608300453-atlas-v9` release moved from repository root to `atlas/releases/`. | The full Git tree probe records the old root path absent and the moved release path present. |
| `CROSSCHECK-004` | `observed` | A GridAtlas overnight study workflow can commit study/candidate output directly to product `main`. | This is an anti-pattern for the audit automation; the new workflow writes only to an audit branch. |
| `CROSSCHECK-005` | `observed` | Companies preserves a compact relationship-candidate and privacy boundary rather than publishing a company master dataset. | The boundary should be retained while adding a separate event-history contract. |
| `CROSSCHECK-006` | `not_observed_in_snapshot` | Claude's `_build-plan` exists at the current GridAtlas commit. | A false tree probe means only that the named path was not observed at this commit; copied study material remains in chatgpt-audits. |

## Governing interpretation

- Agreement between copied study and current source raises confidence but does not constitute human review.
- A contradiction is preserved in the ledger; the older text is not silently rewritten.
- `not_observed_in_snapshot` is not evidence of non-existence.
- The highest-priority seam remains PipelineNews's producer URL versus GridAtlas's current stable route.
- No repair is installed here; the output is a quarantined graduation candidate only.
