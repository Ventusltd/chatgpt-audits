# PipelineNews intelligence engine vNext — quarantined overnight candidate

> **REVIEW STATUS: UNREVIEWED**  
> Classification: `inferred`  
> Quarantined audit output. It is not installed, trusted or published.


## Commercial invariant

The engine is a timing engine for grid-connection sales. It remains silent until a reviewed project identity has both an observed organisation-level funding event and an independently observed planning/procurement event. News explains; it does not manufacture either register fact.

## Module boundary

```text
source collectors
  → append-only evidence ledger
    → reviewed identity binder
      → funding lane + procurement lane
        → deterministic timing state machine
          → human-reviewed read model
            → PipelineNews + GridAtlas consumers
```

## Included proof material

- Four draft JSON schemas with strict fields and no `PUBLISHED` state.
- A deterministic reference timing engine.
- 100,000-sequence adversarial proof, when its required lane completed.
- A top-level-only `repd_ref` extractor and nested-relationship decoy fixture.

This directory is not a drop-in product release. Graduate one reviewed interface or test at a time into its owning repository.
